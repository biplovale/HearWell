#include "Volume.h"

Volume vol;

void setup() {
  vol.begin(); //default pin = D5; alternate = D6

  Serial.begin(9600);
}

void loop() {
  
  //scaling from 0 - 1023 to 0 - 255
//  int testVolume = (analogRead(A1) / 1023.0) * 255; 

  //scaling from 0 - 1023 to 31 - 16000
//  int testFrequency = 31 + ((analogRead(A2)*(1.0 / 1023.0)) * (16000 - 31));

  //Scaling using map
  int frequencyOutputValue = analogRead(A2);
//  int testFrequency = map(frequencyOutputValue, 0, 1023, 31, 16000);

  Serial.println(frequencyOutputValue);

  vol.tone(frequencyOutputValue, 255);

  delay(1);
}
